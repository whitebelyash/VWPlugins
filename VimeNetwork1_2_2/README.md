# VimeNetwork

The main VimeWorld plugin. Requires some "Core" for extra things which is not available.

Probably has different versions across the leaks, but I can't get access to the full `dron.tar` archive for now.
